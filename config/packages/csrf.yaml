# Session-based CSRF for Symfony forms (standard POST body validation).
# Stateless CSRF (cookie + header) is only used for login via _csrf_token field.
framework:
    form:
        csrf_protection:
            token_id: submit

    csrf_protection:
        stateless_token_ids: []
